# Contracts

Minimal Foundry setup for TrustDrop.

## Commands

```bash
cd contracts
forge build
forge test
```

## Deploy

Create a `.env` with `MONAD_RPC_URL` and a private key, then add a deploy script.
